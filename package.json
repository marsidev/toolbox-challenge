{
	"name": "toolbox-challenge",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"api:dev": "pnpm run --filter=api dev",
		"api:start": "pnpm run --filter=api start",
		"api:test": "pnpm run --filter=api test",
		"app:dev": "pnpm run --filter=app dev",
		"app:start": "pnpm run --filter=app start"
	},
	"devDependencies": {
		"standard": "17.0.0"
	},
	"eslintConfig": {
		"extends": [
			"./node_modules/standard/eslintrc.json"
		]
	}
}
